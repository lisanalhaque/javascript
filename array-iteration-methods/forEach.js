// used to iterate over an array and execute a function for each element in the array
// does not return a new array
// does not change the original array
const numbers =[1,2,3];

numbers.forEach(num =>{
    console.log(num);
    
})